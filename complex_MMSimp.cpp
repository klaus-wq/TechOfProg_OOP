#include "complex_atd.h"
using namespace std;
namespace simple_numbers {
	//выводим простую дробь и комплексное число
	void complex::MMSimp(ofstream& ofst) {
		ofst << "Simple and Complex" << endl;
	}
}